# ARMAS Git運用手順

作業ブランチ: `mknimknij`

---

## 1. コミット手順（最新を取り込んでからコミット）

```bash
# 1. 作業対象のブランチに切り替える
#    （すでにmknimknijにいる場合は不要）
git checkout mknimknij

# 2. コミットしていない修正を一時的に退避する
#    未コミットの変更が残ったままpullすると失敗することがあるための予防措置。
#    修正がなければ何も起こらないので、毎回実行してOK。
git stash

# 3. リモートリポジトリ(GitHub)の最新情報を取得する
#    ※ この時点ではまだ自分の作業ブランチには反映されない
git fetch origin

# 4. リモートのmknimknijブランチの最新内容を、自分のブランチに取り込む
git pull origin mknimknij

# 5. 手順2で退避していた修正を元に戻す
#    ※ 同じファイルの同じ箇所が変更されているとコンフリクト（競合）が起きる場合がある
git stash pop

# 6. 変更されたファイルの一覧を確認する
git status

# 7. 変更内容を具体的に確認する（差分表示）
#    表示から抜けるときは "q" キーを押す
git diff

# 8. 変更をステージング（次のコミット対象として登録）する
git add .

# 9. コミットする（変更内容の説明を書く）
#    メッセージは後で見て分かるように具体的に書く
git commit -m "変更内容の説明をここに"

# 10. GitHub上のmknimknijブランチにpushする
git push origin mknimknij
```

### 補足
- `git stash` / `git stash pop` は、作業中の変更を一時的に脇に置いておく操作。
- コンフリクトが起きた場合は、該当ファイルを開いて `<<<<<<<` `=======` `>>>>>>>` のマークがある箇所を手動で修正し、`git add .` → `git commit` で解決する。

---

## 2. GitHubでプルリクエスト（PR）を作成する手順

1. **ブラウザでリポジトリを開く**
   - 例: `https://github.com/ユーザー名/ARMAS`

2. **プルリクエスト作成画面を開く**
   - push直後であれば「Compare & pull request」というバナーが表示されるのでクリック
   - 表示されない場合は、上部の「Pull requests」タブ →「New pull request」

3. **ブランチを選択する**
   - `base`（マージ先。通常は `main`）
   - `compare`（自分の変更元。`mknimknij`）
   - 差分（diff）が表示されるので、意図した変更のみが含まれているか確認する

4. **タイトルと説明を書く**
   - 何を変更したか、なぜ変更したかを簡潔に記載
   - 例:
     ```
     タイトル: 予約への返信機能を追加

     内容:
     - repliesテーブルを追加し、予約ごとに返信を保存できるように変更
     - 返信時にDiscordへ通知するメッセージ形式を追加
     - 動作確認済み
     ```

5. **レビュワーを指定する（任意）**
   - 右側の「Reviewers」欄でオーナーを選択すると、通知が届く

6. **「Create pull request」をクリックして作成完了**

7. **オーナーの確認・マージを待つ**
   - コメントや修正依頼が来た場合は、同じブランチ（mknimknij）に追加でコミット＆pushすれば、自動的にPRに反映される
